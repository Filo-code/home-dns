# Technical Debt Register

Known, accepted compromises. Each entry has an owner decision and a trigger for reassessment.

| ID | Recorded | Item | Impact | Decision | Reassess when |
|---|---|---|---|---|---|
| TD-001 | 2026-09-13 | Starlette `StarletteDeprecationWarning`: "Using `httpx` with `starlette.testclient` is deprecated; install `httpx2` instead" (also an anyio `BlockingPortal` alias deprecation) | Two warnings in every test run; no functional impact | Keep the approved `httpx` dependency; do not change dependencies only to silence warnings | Next dependency maintenance/update cycle, or if a Starlette/FastAPI release removes httpx support |
| TD-002 | 2026-09-13 | Some macOS setups flag `.venv/**/*.pth` as hidden; recent Python skips them, breaking the editable install | `uv run home-dns` fails by hand; `make` targets and pytest are unaffected (explicit `PYTHONPATH=src`) | Keep the workaround documented in `docs/development.md` | If uv or CPython changes `.pth` handling |
| TD-003 | 2026-09-13 | Domain normalization uses Python's built-in `idna` codec (IDNA 2003) for non-ASCII input | Rare internationalized names may normalize differently from IDNA 2008 resolvers | A2 measurement: HaGeZi ships punycode only (84 + 197 `xn--` rules, no raw Unicode), so the codec is never used for these lists. Keep as is | A new list or manual rules with raw Unicode names |
| ~~TD-004~~ | 2026-09-13 | ~~Artifact store keeps every activated artifact~~ | — | **Resolved 2026-09-13:** retention keeps current/previous/backup and prunes atomically after the state write | — |
| TD-005 | 2026-09-14 | Ten protected namespaces are verified by DNS existence only (`googlevideo.com`, `nflxvideo.net`, `aiv-cdn.net`, `aiv-delivery.net`, `pv-cdn.net`, `dssott.com`, `twitchcdn.net`, `steamserver.net`, `steamcontent.com`, `discord.media`) | Ownership not proven by official documentation | Accepted: widely used service domains, no listed names inside the subtrees; flagged in each `source` | Stage C/D device testing (observe real traffic) |
| TD-006 | 2026-09-14 | Prime Video `api.*.aiv-delivery.net` is blocked by every HaGeZi level, including Light | Possible playback issues on Prime Video (unconfirmed) | Not protected without evidence; observe on real devices | Stage C/D; add an expiring allow rule if breakage is confirmed |
| TD-007 | 2026-09-14 | Sanity limits for `hagezi-light` / `hagezi-normal` are not configured | The pipeline applies all hard gates but no size/delta anomaly review for these two sources | Values proposed in the A3 evaluation report | Owner approval; re-measure ≥ 30 days before C3 |
| TD-008 | 2026-09-14 | Smart TV brands are unknown | Vendor-specific endpoints (Samsung/LG/Android TV) are only on a watch list, not protected | No list blocks them today; the watch list is re-checked by the evaluation script | After the Stage B inventory of devices |
| TD-009 | 2026-09-14 | Backups are uncompressed file copies | More disk per backup than a compressed archive | Deliberate (A4 design §11): simpler to verify and partially recover per-file | If backup-directory size on the real Pi becomes a measured problem |
| ~~TD-010~~ | 2026-09-14 | ~~`retention.query_history_days` is a policy value only~~ | — | **Resolved 2026-09-14 (A7):** it bounds the hourly rollups; raw queries are never copied (ADR 0005) | — |
| TD-011 | 2026-09-14 | Login rate limiting is in memory, keyed by client IP and username | A restart clears lockouts; anyone on the LAN can lock a username for 15 min; behind a reverse proxy all clients share one IP | Accepted for a LAN-only household dashboard with a 12-character password minimum | C4, when a reverse proxy/TLS is added (trusted proxy headers), or if lockouts are abused |
| TD-012 | 2026-09-14 | Device activity (top domains, recent queries) scans at most 2 000 provider entries per request | Busy devices show top domains from the most recent part of the 24 h window; the response says `truncated` | Keeps one request bounded on the Pi | C2, after measuring `/api/queries` cost against real Pi-hole data |

